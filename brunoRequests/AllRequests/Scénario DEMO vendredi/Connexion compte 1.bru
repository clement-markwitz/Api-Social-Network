meta {
  name: Connexion compte 1
  type: http
  seq: 2
}

post {
  url: {{url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "{{username_compte_n1}}", 
    "password": "GrosSecret1234!"
  }
}

script:post-response {
  bru.setEnvVar("token_compte_n1", res.body.token);
}
