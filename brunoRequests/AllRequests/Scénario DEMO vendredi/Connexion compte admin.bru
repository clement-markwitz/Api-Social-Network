meta {
  name: Connexion compte admin
  type: http
  seq: 19
}

post {
  url: {{url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "{{username_compte_admin}}", 
    "password": "GrosSecret1234!"
  }
}

script:post-response {
  bru.setEnvVar("token_compte_admin", res.body.token);
}
