meta {
  name: Connexion compte n°2
  type: http
  seq: 6
}

post {
  url: {{url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "{{username_compte_n2}}", 
    "password": "GrosSecret1234!"
  }
}

script:post-response {
  bru.setEnvVar("token_compte_n2", res.body.token);
}
