meta {
  name: conversation
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/conversations
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token_userA}}
}

body:json {
  {
    "name": "Projet Secret",
    "memberIds": ["id"]
  }
}

script:post-response {
  bru.setEnvVar("convId", res.body.id);
}
